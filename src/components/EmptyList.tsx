import { Text, View } from "react-native";
import React from "react";

export const EmptyList = () => (
  <View className="flex-1 items-center justify-center mt-12">
    <Text className="text-lg font-inter-bold">No characters found</Text>
  </View>
);
